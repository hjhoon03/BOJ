#include <iostream>

using namespace std;

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);

    int n;
    int h[100000];
    int max;

    do {
        cin >> n;
        max = 0;

        for(int i = 0; i < n; ++i) {
            cin >> h[i];
        }

        for(int i = 0; i < n; ++i) {

        }

    } while(n != 0);
}